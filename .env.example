# Optional — enables LLM-assisted withdrawal clause judgment in M1
# Without this, M1 falls back to regex detection (confidence: medium)
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...
BROWSER_WS_ENDPOINT=wss://chrome.browserless.io?token=YOUR_TOKEN_HERE

# Adyntel API key for ad spend checks
ADYNTEL_API_KEY=....
ADYNTEL_EMAIL=...